name: account_cleanup
description: Cron job that permanently deletes accounts marked for deletion after 30 days.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  dart_appwrite: ^13.0.0
